export default function Hello({nombre, react}) {
    react = "react"
return(
        <section>
            <div>
            <h2>Módulo 01 — JSX y props básicos</h2>
            <p>Hola, {nombre}</p>
            <p>Bienvenido al primer modulo de {react}.</p>
        </div>
        </section>

)}
